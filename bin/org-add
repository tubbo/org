#!/bin/zsh
#
# Add a new file to the index

zparseopts -K -- t:=title

local filename=$1

if [[ -z $title ]]
then
  local title="$(echo $filename | sed 's/\.org//')"
fi

echo "* [[file:$filename|$title]]" >> index.org

org commit
