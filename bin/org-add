#!/bin/zsh
#
# Add a new file to the index

local filename=$2
local title=$3

if [ -z "$title" ]
then
  local title=$(echo $filename | sed 's/\.org//g')
fi

echo "* [[file:$filename|$title]]" >> $ORG_PATH/index.org

org commit
