#!/bin/zsh
#
# Org mode helpers for the shell

if [[ -n "${ORG_PATH+x}" ]]
then
  # nothing
else
  export ORG_PATH="$HOME/Dropbox/Apps/MobileOrg"
fi

if [[ -f "$(dirname $0:A)/org-$1" ]]
then
  source "$(dirname $0:A)/org-$1";
  exit 0
else
  echo "Command '$1' not found";
  exit 1
fi

